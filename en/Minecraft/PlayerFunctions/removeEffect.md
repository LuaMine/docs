# player:removeEffect(name)
Removes a potion effect from the player, if they have it.

| Param  | Type   | Description                                     |
|--------|--------|---------------------------------------------------|
| `name` | string | Effect name (Bukkit `PotionEffectType`, e.g. `"SPEED"`) |

```lua
player:removeEffect("SPEED")
```
