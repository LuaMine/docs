# mc.broadcast(message)
Broadcasts a message to all online players.

| Param     | Type   | Description                     |
|-----------|--------|---------------------------------|
| `message` | string | Message (supports `&` color codes) |

```lua
mc.broadcast("&aWelcome to the server!")
```