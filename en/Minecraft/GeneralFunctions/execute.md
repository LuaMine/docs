# mc.execute(command)
Executes a console command as the server operator.

| Param     | Type   | Description        |
|-----------|--------|--------------------|
| `command` | string | Console command    |

```lua
mc.execute("give @a diamond 1")
```
