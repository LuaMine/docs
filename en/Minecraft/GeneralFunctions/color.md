# mc.color(text)
Converts `&` color codes into Minecraft color codes.

| Param  | Type   | Description                  |
|--------|--------|------------------------------|
| `text` | string | Text with `&` color codes    |

**Returns:** string

```lua
local colored = mc.color("&cRed text &aGreen text")
```