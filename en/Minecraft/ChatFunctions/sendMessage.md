# mc.sendMessage(player, message)
Sends a message to a specific player.

| Param     | Type     | Description                     |
|-----------|----------|---------------------------------|
| `player`  | userdata | Player object                   |
| `message` | string   | Message (supports `&` color codes) |

```lua
mc.sendMessage(player, "&aHello!")
```
