# mc.execute(command)
Выполняет консольную команду от имени сервера.

| Параметр  | Тип      | Описание          |
|-----------|----------|-------------------|
| `command` | string   | Консольная команда|

```lua
mc.execute("give @a diamond 1")
```
