# mc.log(message)
Записывает сообщение в консоль сервера.

| Параметр  | Тип      | Описание                    |
|-----------|----------|-----------------------------|
| `message` | string   | Сообщение для записи        |

```lua
mc.log("Привет из Lua!")
```